{
  "name": "vsts-task-code-push-release",
  "version": "1.2.2",
  "description": "Release an app update to the CodePush service",
  "main": "codepush-release.js",
  "scripts": {
    "deploy": "tfx build tasks upload --task-path ./ --overwrite",
    "test": "mocha test.js"
  },
  "author": "Microsoft Corporation",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^2.8.0",
    "code-push-cli": "^1.10.0-beta"
  },
  "devDependencies": {
    "sinon": "1.17.2",
    "assert": "1.3.0",
    "mocha": "~2.4.5"
  }
}
